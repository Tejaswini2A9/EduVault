<!DOCTYPE html>
<html lang="en" data-bs-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Certificate Management - EduVault</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color: #4361ee;
            --secondary-color: #3a0ca3;
            --accent-color: #4cc9f0;
            --success-color: #2ecc71;
            --warning-color: #f39c12;
            --danger-color: #e74c3c;
            --light-bg: #f8f9fa;
            --dark-bg: #212529;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--light-bg);
            min-height: 100vh;
            overflow-x: hidden;
        }

        .navbar {
            background: linear-gradient(135deg, var(--secondary-color), var(--primary-color));
            box-shadow: 0 4px 20px rgba(0,0,0,0.1);
            padding: 12px 0;
        }

        /* Navbar text styling */
        .navbar h2 {
            color: white;
            font-weight: 600;
            margin-bottom: 0;
            font-size: 1.5rem;
        }

        .navbar h2 span {
            font-weight: 400;
            opacity: 0.9;
            color: white;
        }

        .navbar ul li a {
            color: white !important;
            font-weight: 500;
            transition: all 0.3s;
            margin: 0 5px;
            padding: 8px 15px !important;
            border-radius: 8px;
        }

        .navbar ul li a:hover, .navbar ul li a.active {
            color: white !important;
            background-color: rgba(255,255,255,0.15);
            transform: translateY(-2px);
        }

        .profile-icon {
            width: 36px !important;
            height: 36px !important;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid white;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }

        /* Dropdown menu styling */
        .dropdown-menu {
            border-radius: 12px;
            box-shadow: 0 5px 20px rgba(0,0,0,0.1);
            border: none;
            padding: 10px;
            background-color: white;
        }

        .dropdown-item {
            border-radius: 8px;
            padding: 8px 15px;
            transition: all 0.2s;
            color: #212529 !important; /* Black text for dropdown items */
        }

        .dropdown-item:hover {
            background-color: rgba(67, 97, 238, 0.1);
            transform: translateX(5px);
            color: #212529 !important; /* Keep text black on hover */
        }

        .dropdown-item.active {
            background-color: rgba(67, 97, 238, 0.2);
            color: #212529 !important; /* Keep text black when active */
        }

        .container {
            padding: 30px 15px;
            max-width: 1400px;
        }

        .page-title {
            color: var(--secondary-color);
            font-weight: 600;
            margin-bottom: 30px;
            position: relative;
            display: inline-block;
            padding-bottom: 10px;
        }

        .page-title:after {
            content: '';
            position: absolute;
            width: 70%;
            height: 3px;
            background: linear-gradient(90deg, var(--primary-color), var(--accent-color));
            bottom: 0;
            left: 0;
            border-radius: 2px;
        }

        .table-container {
            background-color: white;
            border-radius: 16px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.08);
            overflow: hidden;
            margin-bottom: 40px;
        }

        .table {
            margin-bottom: 0;
        }

        .table thead {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
        }

        .table thead th {
            font-weight: 600;
            border: none;
            padding: 15px;
            white-space: nowrap;
            vertical-align: middle;
        }

        .table tbody tr {
            transition: all 0.3s;
        }

        .table tbody tr:hover {
            background-color: rgba(67, 97, 238, 0.05);
        }

        .table tbody td {
            padding: 15px;
            vertical-align: middle;
            border-color: rgba(0,0,0,0.05);
        }

        .table-header {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
            padding: 20px;
            border-radius: 16px 16px 0 0;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .table-header h2 {
            margin-bottom: 0;
            font-weight: 600;
            font-size: 1.5rem;
        }

        .btn {
            border-radius: 8px;
            padding: 8px 16px;
            font-weight: 500;
            transition: all 0.3s;
        }

        .btn-success {
            background: linear-gradient(135deg, #2ecc71, #27ae60);
            border: none;
        }

        .btn-success:hover {
            background: linear-gradient(135deg, #27ae60, #2ecc71);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(46, 204, 113, 0.3);
        }

        .btn-danger {
            background: linear-gradient(135deg, #e74c3c, #c0392b);
            border: none;
        }

        .btn-danger:hover {
            background: linear-gradient(135deg, #c0392b, #e74c3c);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(231, 76, 60, 0.3);
        }

        .btn i {
            margin-right: 5px;
        }

        .table-responsive {
            overflow-x: auto;
            scrollbar-width: thin;
            scrollbar-color: var(--primary-color) #f1f1f1;
        }

        .table-responsive::-webkit-scrollbar {
            height: 8px;
        }

        .table-responsive::-webkit-scrollbar-track {
            background: #f1f1f1;
            border-radius: 10px;
        }

        .table-responsive::-webkit-scrollbar-thumb {
            background: var(--primary-color);
            border-radius: 10px;
        }

        .table-responsive::-webkit-scrollbar-thumb:hover {
            background: var(--secondary-color);
        }

        .certificate-count {
            background-color: white;
            border-radius: 16px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.08);
            padding: 20px;
            margin-bottom: 30px;
            display: flex;
            align-items: center;
        }

        .certificate-count-icon {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--primary-color), var(--accent-color));
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 20px;
            color: white;
            font-size: 24px;
        }

        .certificate-count-text h3 {
            font-weight: 600;
            margin-bottom: 5px;
            color: var(--secondary-color);
        }

        .certificate-count-text p {
            margin-bottom: 0;
            color: #6c757d;
        }

        .empty-state {
            text-align: center;
            padding: 50px 20px;
            background-color: white;
            border-radius: 16px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.08);
        }

        .empty-state-icon {
            font-size: 60px;
            color: var(--primary-color);
            margin-bottom: 20px;
        }

        .empty-state h3 {
            font-weight: 600;
            margin-bottom: 10px;
            color: var(--secondary-color);
        }

        .empty-state p {
            color: #6c757d;
            max-width: 500px;
            margin: 0 auto 20px;
        }

        .weblink-cell {
            max-width: 200px;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
        }

        .weblink-cell a {
            color: var(--primary-color);
            transition: all 0.3s;
        }

        .weblink-cell a:hover {
            color: var(--secondary-color);
            text-decoration: none;
        }

        .action-buttons .btn {
            margin-bottom: 5px;
        }

        @media (max-width: 992px) {
            .table-container {
                width: 100%;
                overflow-x: auto;
            }

            .container {
                padding: 20px 10px;
            }
        }
    </style>
</head>
<body>
    {{#each std_data}}
    <nav class="navbar navbar-expand-lg sticky-top">
        <div class="container-fluid px-4">
            <h2><i class="bi bi-mortarboard me-2"></i>Welcome <span>{{this.Student_id}}</span></h2>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                    aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/{{this.Student_id}}/homepage">
                            <i class="bi bi-house-door me-1"></i> Home
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/{{this.Student_id}}/upload">
                            <i class="bi bi-upload me-1"></i> Upload
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/{{this.Student_id}}/certificate">
                            <i class="bi bi-award me-1"></i> Certificate
                        </a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <img src="/uploaded_images/{{this.pp}}" alt="Profile" class="profile-icon me-2">
                            <span>Profile</span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><a class="dropdown-item" href="/{{this.Student_id}}/settings">
                                <i class="bi bi-gear me-2"></i> Settings
                            </a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="/logout">
                                <i class="bi bi-box-arrow-right me-2"></i> Logout
                            </a></li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    {{/each}}

    <div class="container">
        <div class="row mb-4">
            <div class="col-12">
                <h1 class="page-title"><i class="bi bi-award me-2"></i>Certificate Management</h1>
            </div>
        </div>

        {{#if files.length}}
        <div class="row mb-4">
            <div class="col-md-4">
                <div class="certificate-count">
                    <div class="certificate-count-icon">
                        <i class="bi bi-file-earmark-check"></i>
                    </div>
                    <div class="certificate-count-text">
                        <h3>{{files.length}}</h3>
                        <p>Total Certificates</p>
                    </div>
                </div>
            </div>
            <div class="col-md-8">
                <div class="d-flex justify-content-end mb-3">
                    <a href="/{{std_data.[0].Student_id}}/upload" class="btn btn-primary">
                        <i class="bi bi-plus-circle me-2"></i>Add New Certificate
                    </a>
                </div>
            </div>
        </div>

        <div class="table-container">
            <div class="table-header">
                <h2><i class="bi bi-list-check me-2"></i>Certificate Details</h2>
            </div>
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th scope="col"><i class="bi bi-person-badge me-1"></i>Roll No</th>
                            <th scope="col"><i class="bi bi-person me-1"></i>Student Name</th>
                            <th scope="col"><i class="bi bi-bookmark me-1"></i>Course Type</th>
                            <th scope="col"><i class="bi bi-book me-1"></i>Course Name</th>
                            <th scope="col"><i class="bi bi-check-circle me-1"></i>Status</th>
                            <th scope="col"><i class="bi bi-calendar-event me-1"></i>Issue Date</th>
                            <th scope="col"><i class="bi bi-building me-1"></i>Organization</th>
                            <th scope="col"><i class="bi bi-currency-dollar me-1"></i>Fee</th>
                            <th scope="col"><i class="bi bi-link-45deg me-1"></i>Web Link</th>
                            <th scope="col"><i class="bi bi-hash me-1"></i>File ID</th>
                            <th scope="col"><i class="bi bi-calendar-plus me-1"></i>Upload Date</th>
                            <th scope="col" colspan="2" class="text-center"><i class="bi bi-gear me-1"></i>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each files}}
                            <tr>
                                <td>{{this.Stu_id}}</td>
                                <td>{{this.student_name}}</td>
                                <td><span class="badge bg-primary">{{this.course_type}}</span></td>
                                <td>{{this.course_name}}</td>
                                <td>
                                    {{#if (eq this.ppc "Completed")}}
                                        <span class="badge bg-success">{{this.ppc}}</span>
                                    {{else if (eq this.ppc "Participated")}}
                                        <span class="badge bg-info">{{this.ppc}}</span>
                                    {{else}}
                                        <span class="badge bg-warning">{{this.ppc}}</span>
                                    {{/if}}
                                </td>
                                <td>{{this.dateoncertificate}}</td>
                                <td>{{this.OrgAgent}}</td>
                                <td>
                                    {{#if (eq this.Fee "FREE")}}
                                        <span class="badge bg-success">{{this.Fee}}</span>
                                    {{else}}
                                        <span class="badge bg-warning">{{this.Fee}}</span>
                                    {{/if}}
                                </td>
                                <td class="weblink-cell">
                                    {{#if this.weblink}}
                                        <a href="{{this.weblink}}" target="_blank" title="{{this.weblink}}">
                                            <i class="bi bi-box-arrow-up-right me-1"></i>View Link
                                        </a>
                                    {{else}}
                                        <span class="text-muted">Not available</span>
                                    {{/if}}
                                </td>
                                <td><span class="badge bg-secondary">{{this.file_id}}</span></td>
                                <td>{{this.Date}}</td>
                                <td class="action-buttons">
                                    <a href="/download/{{this.file_id}}" class="btn btn-success btn-sm w-100">
                                        <i class="bi bi-download"></i> Download
                                    </a>
                                </td>
                                <td class="action-buttons">
                                    <a href="/{{this.Stu_id}}/delete-file/{{this.file_id}}" class="btn btn-danger btn-sm w-100"
                                       onclick="return confirm('Are you sure you want to delete this certificate?')">
                                        <i class="bi bi-trash"></i> Remove
                                    </a>
                                </td>
                            </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        </div>
        {{else}}
        <div class="empty-state">
            <div class="empty-state-icon">
                <i class="bi bi-award"></i>
            </div>
            <h3>No Certificates Found</h3>
            <p>You haven't uploaded any certificates yet. Start by adding your first certificate.</p>
        </div>
        {{/if}}
    </div>

    <!-- Bootstrap JS and Popper.js -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>
</body>
</html>
