<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin - Certificate Details</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.3/font/bootstrap-icons.css">
    <style>
        :root {
            --primary-color: #4e73df;
            --secondary-color: #858796;
            --success-color: #1cc88a;
            --info-color: #36b9cc;
            --warning-color: #f6c23e;
            --danger-color: #e74a3b;
            --light-color: #f8f9fc;
            --dark-color: #5a5c69;
        }
        
        body {
            background-color: #f8f9fc;
            font-family: 'Nunito', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
        }
        
        .navbar {
            background-color: white;
            box-shadow: 0 0.15rem 1.75rem 0 rgba(58, 59, 69, 0.15);
        }
        
        .navbar h2 {
            color: var(--dark-color);
            font-weight: 700;
        }
        
        .navbar h2 span {
            color: var(--primary-color);
        }
        
        .nav-link {
            color: var(--secondary-color);
            font-weight: 600;
            padding: 0.5rem 1rem;
            border-radius: 0.35rem;
            margin: 0 0.25rem;
        }
        
        .nav-link:hover, .nav-link.active {
            color: var(--primary-color);
            background-color: #eaecf4;
        }
        
        .page-title {
            color: var(--dark-color);
            font-weight: 700;
            margin-bottom: 1.5rem;
        }
        
        .card {
            border: none;
            border-radius: 0.35rem;
            box-shadow: 0 0.15rem 1.75rem 0 rgba(58, 59, 69, 0.15);
            margin-bottom: 1.5rem;
        }
        
        .card-header {
            background-color: #f8f9fc;
            border-bottom: 1px solid #e3e6f0;
            padding: 1rem 1.25rem;
        }
        
        .card-header h6 {
            font-weight: 700;
            color: var(--primary-color);
            margin: 0;
        }
        
        .detail-container {
            background-color: white;
            border-radius: 0.35rem;
            box-shadow: 0 0.15rem 1.75rem 0 rgba(58, 59, 69, 0.15);
            margin-bottom: 1.5rem;
            padding: 1.5rem;
        }
        
        .detail-header {
            padding-bottom: 1rem;
            margin-bottom: 1.5rem;
            border-bottom: 1px solid #e3e6f0;
        }
        
        .detail-header h2 {
            font-weight: 700;
            color: var(--dark-color);
            margin: 0;
            font-size: 1.25rem;
        }
        
        .detail-row {
            margin-bottom: 1rem;
            padding-bottom: 1rem;
            border-bottom: 1px solid #f8f9fc;
        }
        
        .detail-label {
            font-weight: 600;
            color: var(--secondary-color);
        }
        
        .detail-value {
            font-weight: 500;
            color: var(--dark-color);
        }
        
        .certificate-preview {
            width: 100%;
            height: 400px;
            border: 1px solid #e3e6f0;
            border-radius: 0.35rem;
            overflow: hidden;
        }
        
        .certificate-preview iframe {
            width: 100%;
            height: 100%;
            border: none;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg sticky-top">
        <div class="container-fluid px-4">
            <h2><i class="bi bi-shield-lock me-2"></i>Admin <span>Dashboard</span></h2>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                    aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/admin/dashboard">
                            <i class="bi bi-house-door me-1"></i> Dashboard
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/admin/users">
                            <i class="bi bi-people me-1"></i> Users
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/admin/certificates">
                            <i class="bi bi-award me-1"></i> Certificates
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/admin/reports/students">
                            <i class="bi bi-file-earmark-text me-1"></i> Reports
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">
                            <i class="bi bi-box-arrow-right me-1"></i> Logout
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container py-4">
        <div class="row mb-4">
            <div class="col-12 d-flex justify-content-between align-items-center">
                <h1 class="page-title"><i class="bi bi-award me-2"></i>Certificate Details</h1>
                <a href="/admin/certificates" class="btn btn-secondary">
                    <i class="bi bi-arrow-left me-1"></i> Back to Certificates
                </a>
            </div>
        </div>

        <div class="row">
            <div class="col-md-6">
                <div class="detail-container">
                    <div class="detail-header">
                        <h2><i class="bi bi-file-earmark-text me-2"></i>Certificate Information</h2>
                    </div>
                    
                    <div class="detail-row">
                        <div class="detail-label">File ID</div>
                        <div class="detail-value">{{certificate.file_id}}</div>
                    </div>
                    
                    <div class="detail-row">
                        <div class="detail-label">Course Type</div>
                        <div class="detail-value">{{certificate.course_type}}</div>
                    </div>
                    
                    <div class="detail-row">
                        <div class="detail-label">Course Name</div>
                        <div class="detail-value">{{certificate.course_name}}</div>
                    </div>
                    
                    <div class="detail-row">
                        <div class="detail-label">Status</div>
                        <div class="detail-value">
                            {{#if (eq certificate.ppc "Completed")}}
                                <span class="badge bg-success">{{certificate.ppc}}</span>
                            {{else if (eq certificate.ppc "Participated")}}
                                <span class="badge bg-info">{{certificate.ppc}}</span>
                            {{else}}
                                <span class="badge bg-warning">{{certificate.ppc}}</span>
                            {{/if}}
                        </div>
                    </div>
                    
                    <div class="detail-row">
                        <div class="detail-label">Issue Date</div>
                        <div class="detail-value">{{formatDate certificate.dateoncertificate}}</div>
                    </div>
                    
                    <div class="detail-row">
                        <div class="detail-label">Organization</div>
                        <div class="detail-value">{{certificate.OrgAgent}}</div>
                    </div>
                    
                    <div class="detail-row">
                        <div class="detail-label">Fee</div>
                        <div class="detail-value">
                            {{#if (eq certificate.Fee "FREE")}}
                                <span class="badge bg-success">{{certificate.Fee}}</span>
                            {{else}}
                                <span class="badge bg-warning">{{certificate.Fee}}</span>
                            {{/if}}
                        </div>
                    </div>
                    
                    <div class="detail-row">
                        <div class="detail-label">Web Link</div>
                        <div class="detail-value">
                            {{#if certificate.weblink}}
                                <a href="{{certificate.weblink}}" target="_blank">{{certificate.weblink}}</a>
                            {{else}}
                                <span class="text-muted">Not available</span>
                            {{/if}}
                        </div>
                    </div>
                    
                    <div class="detail-row">
                        <div class="detail-label">Upload Date</div>
                        <div class="detail-value">{{formatDate certificate.Date}}</div>
                    </div>
                    
                    <div class="d-grid gap-2 d-md-flex justify-content-md-end mt-4">
                        <a href="/download/{{certificate.file_id}}" class="btn btn-success me-md-2">
                            <i class="bi bi-download me-1"></i> Download Certificate
                        </a>
                        <form action="/admin/certificates/{{certificate.file_id}}/delete" method="POST" onsubmit="return confirm('Are you sure you want to delete this certificate?')">
                            <button type="submit" class="btn btn-danger">
                                <i class="bi bi-trash me-1"></i> Delete Certificate
                            </button>
                        </form>
                    </div>
                </div>
            </div>
            
            <div class="col-md-6">
                <div class="detail-container">
                    <div class="detail-header">
                        <h2><i class="bi bi-person-vcard me-2"></i>Student Information</h2>
                    </div>
                    
                    <div class="detail-row">
                        <div class="detail-label">Student ID</div>
                        <div class="detail-value">{{student.Student_id}}</div>
                    </div>
                    
                    <div class="detail-row">
                        <div class="detail-label">Student Name</div>
                        <div class="detail-value">{{student.student_name}}</div>
                    </div>
                    
                    <div class="detail-row">
                        <div class="detail-label">Email</div>
                        <div class="detail-value">{{student.Email}}</div>
                    </div>
                    
                    <div class="detail-row">
                        <div class="detail-label">Phone Number</div>
                        <div class="detail-value">{{student.Phone_number}}</div>
                    </div>
                    
                    <div class="d-grid gap-2 d-md-flex justify-content-md-end mt-4">
                        <a href="/admin/users/{{student.Student_id}}/edit" class="btn btn-primary">
                            <i class="bi bi-pencil me-1"></i> Edit Student
                        </a>
                    </div>
                </div>
                
                <div class="detail-container">
                    <div class="detail-header">
                        <h2><i class="bi bi-file-earmark-pdf me-2"></i>Certificate Preview</h2>
                    </div>
                    
                    <div class="certificate-preview">
                        <iframe src="/download/{{certificate.file_id}}" title="Certificate Preview"></iframe>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS and Popper.js -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>
</body>
</html>
